import React from 'react';

const PlaceOrder = () => {
    return (
        <div style={{textAlign:"center"}}>
            <h1>Your order is done</h1>
            </div>
    );
};

export default PlaceOrder;